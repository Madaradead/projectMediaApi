 # Server settings
 PORT=5000

 # Secret for generating JWT tokens (replace with your own in the real .env)
 JWT_SECRET=your_super_secret_jwt_key_here

 # PostgreSQL (Prisma) database connection string
 DATABASE_URL="postgresql://user:password@localhost:5432/media_db?schema=public"